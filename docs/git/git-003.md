## 2026-03-01 | feature/files-vaults: Supabase Vault Integration

**Branch:** `feature/files-vaults`  
**Status:** In Progress 🚧

### Scope
- Migrated all schema IDs from `uuid_generate_v4()` to `gen_random_uuid()`.
- Implemented `vaultStore.ts` (Zustand) and `useFileContent` hook for real data fetching.
- Integrated `VaultSidebar` with Supabase: tree building, rename/delete, and inline creation.
- Added database migrations for vault folders hierarchy and RLS policies.
- Fixed SVG attribute naming conventions in `Dock.tsx` (React compatibility).

### Files Added
- `shared/types/vault.ts`
- `frontend/src/features/vault/store/vaultStore.ts`
- `backend/supabase/migrations/2026030100000[0-2]_*.sql`
- `frontend/src/lib/supabase.ts`
